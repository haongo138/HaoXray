extends ../layouts/common
block content
	h1 Login
	if errors
			each error in errors
				.alert.alert-danger= error
	.container
		form(method="POST")
			.form-group.row
				label.col-sm-2.form-control-label(for='inputEmail3') Username
				.col-sm-10
					input#inputEmail3.form-control(type='text', placeholder='Username', name='username', value=value ? value.username : "")
			.form-group.row
				label.col-sm-2.form-control-label(for='inputPassword3') Password
				.col-sm-10
					input#inputPassword3.form-control(type='password', placeholder='Password', name='password')
			button.btn.btn-primary Login
		.alert.alert-warning(role='alert')
			|  If you don&apos;t have an account? 
			a(href='/users/register')
				strong Register
